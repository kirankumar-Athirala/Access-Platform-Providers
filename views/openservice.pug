doctype html
head
 link(rel="icon", href="/images/favicon.ico")
 style.
  body { 
    margin: 0;  
    padding: 0;  
    background-color:#6abadeba;
    font-family: 'Arial';  
  } 
  table, th, td {
  border:1px solid blue;
  }
h1.text-center.top-buffer Access Management Platform   
h1.text-center.top-buffer #{title}
table(style='width:100%')
  tr
    th position ID
    th agreements ID
    th Name 
    th ADD
  each detail in details
    tr
    td #{detail._id}
    td #{detail.agreementsId}
    td #{detail.PositionName}
    td
     li
      a(href=`/users/offerEmployee?positionid=${detail._id}&agreementsid=${detail.agreementsId}`) offerEmployee